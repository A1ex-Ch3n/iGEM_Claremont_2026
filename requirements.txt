# iGEM Claremont 2026 — root requirements.txt
# ─────────────────────────────────────────────
# RECOMMENDED: use conda env instead (handles hmmer, pyrodigal, torch better)
#   conda env create -f shared/env/environment.yml
#   conda activate igem2026
#
# This file is for quick pip-only installs or CI environments.
# 推薦使用 conda（見上），這個檔案適合純 pip 環境或 CI。

# ── Data science core ──────────────────────────────────────────────
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.0
matplotlib>=3.6
seaborn>=0.12

# ── Bioinformatics ─────────────────────────────────────────────────
biopython>=1.83
pyrodigal>=3.0          # Module 02: bacterial ORF calling
# hmmer: install via brew (brew install hmmer) or conda (conda install -c bioconda hmmer)
# NOT pip-installable
xgboost>=1.6            # Module 03: ML track classifier
phanotate>=1.6.7        # Module 02: phage ORF caller

# ── Deep learning ──────────────────────────────────────────────────
torch>=2.0              # CPU build — for GPU see: https://pytorch.org/get-started
fair-esm==2.0.0         # Module 04: ESM-2 protein language model

# ── Module 05: Boltz-2 structure prediction ────────────────────────
# NOTE: boltz has complex pinned deps; install in this order:
#   pip install boltz==2.0.3 --no-deps
#   pip install pytorch-lightning==2.5.0 hydra-core==1.3.2 einops==0.8.0 einx==0.3.0 fairscale==0.4.13
#   pip install dm-tree gemmi==0.6.5 mashumaro==3.14 modelcif==1.2 ihm chembl-structure-pipeline rdkit
boltz==2.0.3
pytorch-lightning==2.5.0
hydra-core==1.3.2
einops==0.8.0
einx==0.3.0
fairscale==0.4.13
dm-tree
gemmi==0.6.5
mashumaro==3.14
modelcif==1.2
ihm
chembl-structure-pipeline>=1.2.2
rdkit

# ── Dev / notebook tools ───────────────────────────────────────────
jupyterlab
ipykernel
nbstripout
nbformat>=5.0
pytest>=7.0
tqdm
requests
openpyxl
wandb==0.18.7
click==8.1.7
